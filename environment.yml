#name: lessdeep
channels:
  - defaults
  - peterjc123  # for pytorch on windows
dependencies:
  - jupyter
  - matplotlib
  - ipywidgets
  - numpy
  - scipy
  - pandas
  - pytorch
  - python-lmdb
  - pip:
    - tqdm
    - keras-tqdm
    - torchvision
