#name: lessdeep
channels:
  - defaults
  - peterjc123  # for pytorch on windows
dependencies:
  - jupyter
  - matplotlib
  - ipywidgets
  - numpy
  - scipy
  - pandas
  - pytorch
  - pip:
    - tqdm
    - keras-tqdm
    - torchvision
